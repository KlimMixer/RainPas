program RainPas;

uses graph;

CONST

TYPE

VAR
        gd, gm : integer;
BEGIN
        gd := detect;
        gm := 0;
        InitGraph(gd, gm, '');
        if GraphResult <> grok then
                halt;
        CloseGraph;
END.

